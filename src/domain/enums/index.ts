export enum TrainingSkillsEnum {
  IdleAttack = 1,
  RegularAttack = 2,
  Block = 10,
  DefensiveBuff = 11,
}

export enum SkillType {
  Defence = 'Defence',
  Attack = 'Attack',
}

export enum Icons {
  Shield = 'md-shield',
  Sword = 'ri-sword-fill',
  Energy = 'md-electricbolt',
}

export enum ItemSlotEnum {
  shield,
  sword,
  helmet,
  chest,
  pants,
  boots,
  amulet,
  ring,
}
